from setuptools import setup

setup(name='custom_snake',
    version='0.0.1',
    authors = ["Lionel", "Arthur", "Aurèle"],
    install_requires=['gym', 'numpy', 'matplotlib'],
    python_requires='>=3'
)